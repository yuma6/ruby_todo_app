<div>
    <%= link_to("index","/task/index") %>
    <h1>edit</h1>

    <div name="todo_edit">
        <%= form_tag("/post/#{params[:id]}/edit") do %>
            <textarea name="content"></textarea>
            <br>
            <input type="submit" value="編集">
        <% end %>
    </div>

    <div name="todo_check">
        <span>作業済チェック</span>
        <input type="checkbox">
    </div>

    <div name="todo_destroy">
        <%= link_to "削除","/post/#{params[:id]}/destroy", method: :post %>
    </div>

</div>